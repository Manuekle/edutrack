export const metadata = {
  title: 'Generador de Asignaturas - FUP',
  description: 'Crea y gestiona asignaturas directamente en la plataforma',
  keywords: 'docente, generador, asignaturas, clases, FUP',
};

export default function GeneradorLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
